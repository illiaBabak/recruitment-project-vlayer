export type Construction = {
  image: string;
  name: string;
  location: string;
  price: string;
  type: 'House' | 'Apartment';
};

export type Testimonial = {
  testimonial: string;
  author: string;
  authorImage: string;
  authorRole: string;
};
